version: '3.8'

services:
  bobot:
    build: .
    container_name: bobot
    env_file:
      - .env
    ports:
      - "11434:11434"
    restart: unless-stopped
    volumes:
      - ollama-data:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0:11434

volumes:
  ollama-data:
